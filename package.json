{
  "name": "samson-landing-page",
  "version": "1.0.0",
  "description": "Samson Group of Companies Landing Page",
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "dev:all": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\" \"npm --prefix web run dev\"",
    "dev:react": "npm --prefix client run dev",
    "dev:nextjs": "npm --prefix web run dev",
    "dev:server": "npm --prefix server run dev",
    "install-all": "npm install && npm --prefix server install && npm --prefix client install && npm --prefix web install",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
